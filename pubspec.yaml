name: aplikasi_cek_resi
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  cloud_firestore: ^5.0.1
  flutter:
    sdk: flutter
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - asset/nira.png
    - asset/anteraja.png
    - asset/id.png
    - asset/j&t.png
    - asset/jne.png
    - asset/ninja.png
    - asset/sicepat.png

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "asset/nira.png"
  min_sdk_android: 21